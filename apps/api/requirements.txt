# CompetitorTrack — api runtime dependencies.
#
# The `api` and `scheduler` services run from the SAME image (§A2), so this one
# file feeds both. Pins are exact: a rebuild on the Oracle A1 (aarch64) must
# resolve to the same versions CI tested on x86_64, and `docker compose build`
# happens on the server with no registry in between (§A1).
#
# Every package below ships manylinux aarch64 wheels or is pure Python, so
# nothing compiles from source on the A1 (§A3). Versions verified 2026-09-22.
#
# The full set is pinned from P1 — not just the parts P1 imports — so the first
# build on the server proves the wheel claim for the whole MVP instead of
# discovering a missing aarch64 wheel in P3 or P4 (see D1.8 in docs/DECISIONS.md).
# The comment on each group says which phase first uses it.

# --- web framework / ASGI (P1) ---
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0

# --- database (P1) ---
SQLAlchemy==2.0.54
alembic==1.20.0
psycopg[binary]==3.3.6
greenlet==3.5.6                   # SQLAlchemy's asyncio bridge needs it explicitly

# --- scheduling (P1) ---
APScheduler==3.11.3               # 3.x only — 4.x is alpha with a different API (§A3)

# --- registrable-domain classification (P1, discharges D0.6) ---
publicsuffixlist==1.0.2.20260921  # bundled PSL data, no network access at import

# --- scraping (P3) ---
httpx[socks]==0.28.1              # proxy= is a constructor arg in 0.28; proxies= was removed

# --- auth (P2) ---
pwdlib[argon2]==0.3.1             # never passlib (§A3)
PyJWT==2.14.0                     # never python-jose — CVE-2024-33663/33664 (§A3)
slowapi==0.1.10                   # in-memory rate limiting, no Redis
email-validator==2.3.0            # pydantic EmailStr backend

# --- secrets at rest (P2) ---
cryptography==50.0.1              # MultiFernet

# --- notifications (P4) ---
aiosmtplib==5.1.3

# --- AI insights (P4) ---
google-genai==2.24.0              # google-generativeai is deprecated (§A3)
